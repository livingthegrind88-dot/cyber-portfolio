#!/usr/bin/env bash
# newwriteup.sh — WIP
# Goal: deploy the FORM-001 incident-report template into writeups/ as a new,
# unnamed buffer so it forces a correct filename on save. Standardizes and
# speeds up write-up creation.
# Status: scaffolding — building this step by step as a learning exercise.

echo "newwriteup.sh — not built yet. See projects/README.md for the plan."

